[pytest]
testpaths = tests
addopts = -q --tb=short --strict-markers
markers =
    live: hits the real Groq API - deselected by default, run with -m live
    model: loads the embedding model (~20s on first use, then cached)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
