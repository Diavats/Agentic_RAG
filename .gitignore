# --- Secrets ---
.env

# --- Python ---
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- Generated artifacts ---
# Narrative stories are regenerated by `python -m src.main ingest` and cost
# one Groq call per row — they are outputs, not source.
data/generated/

# --- Built indexes ---
# Currently ignored because the index on disk was built with the colliding
# IDs fixed in Fix 2 and the 500-token chunks fixed in Fix 3 — it is stale.
# AFTER Phase 4 rebuilds it, DELETE the next line and commit chroma_store/:
# the deployed Render instance serves a pre-built, read-only index baked into
# the image (Render's free tier has an ephemeral filesystem), and ingestion
# stays an offline admin operation per PRD section 3.
chroma_store/

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
